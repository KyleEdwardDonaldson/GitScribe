# Created using wingetcreate
# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json

PackageIdentifier: KyleDonaldson.GitScribe.Status
PackageVersion: 0.1.0
Platform:
  - Windows.Desktop
MinimumOSVersion: 10.0.0.0
Scope: machine
InstallModes:
  - interactive
  - silent
  - silentWithProgress
UpgradeBehavior: install
Installers:
  - Architecture: x64
    InstallerType: exe  # Change to 'msi' or 'inno' based on your installer
    InstallerUrl: https://github.com/KyleEdwardDonaldson/GitScribe/releases/download/status-v0.1.0/GitScribeStatus-Setup.exe
    InstallerSha256: REPLACE_WITH_ACTUAL_SHA256_HASH
    # Silent install args depend on installer type:
    # NSIS: /S
    # Inno Setup: /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
    # MSI: /quiet /norestart
    InstallerSwitches:
      Silent: /VERYSILENT /SUPPRESSMSGBOXES /NORESTART
      SilentWithProgress: /SILENT /SUPPRESSMSGBOXES /NORESTART
ManifestType: installer
ManifestVersion: 1.6.0
